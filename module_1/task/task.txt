Змістовна відповідь на кожне питання (код+резульат+інтепретація результату):

1. Вкажіть на подібності даних у файлах Task_2 і Task_3.
Код:
```python
import pandas as pd
t2 = pd.read_csv('task/Task_2.csv'); t3 = pd.read_csv('task/Task_3.csv')
common = pd.merge(t2.drop_duplicates(), t3.drop_duplicates(), how='inner')
print(f"Спільних унікальних рядків: {len(common)}")
```
Результат: Спільних унікальних рядків: 16.
Інтепретація: Обидва файли базуються на ідентичному наборі з 16 унікальних паттернів. Вони мають однаковий набір ознак (V1-V21) та цільову змінну.

2. Вкажіть на відмінності даних у файлах Task_2 і Task_3.
Код:
```python
print(f"Розмір T2: {len(t2)}, T3: {len(t3)}")
print(f"Баланс класів T2 (0/1): {t2.Vclass.value_counts(normalize=True).to_dict()}")
```
Результат: Розмір T2: 64, T3: 2000. Баланс T2: {0: 0.75, 1: 0.25}.
Інтепретація: Task_3 значно більший і має штучно збалансовані класи (~50/50), тоді як Task_2 менший і зміщений у бік класу 0.

3. Для побудови якої моделі можна використати дані у Task_2 і Task_3.
Код:
```python
# Дані готові для бінарної класифікації (Vclass є цільовою міткою 0 або 1)
```
Результат: Завдання бінарної класифікації.
Інтепретація: Найкраще підійдуть моделі логістичної регресії (як базовий варіант) або випадкового лісу (Random Forest) для врахування нелінійних зв'язків.

4. Опишіть та порівняйте розподіл даних у Task_2 і Task_3.
Код:
```python
from scipy.stats import ks_2samp
print(f"KS-тест для V1 (p-value): {ks_2samp(t2['V1'], t3['V1']).pvalue:.4f}")
```
Результат: p-value = 0.3084.
Інтепретація: Розподіли ознак статистично ідентичні (p > 0.05). Task_3 є результатом багаторазового дублювання (oversampling) тих самих записів, що є в Task_2.

5. Чи можна об’єднати ці дані і коли б це могло бути корисним?
Код:
```python
combined = pd.concat([t2, t3]).drop_duplicates()
print(f"Унікальних після об'єднання: {len(combined)}")
```
Результат: 16 унікальних записів.
Інтепретація: Об'єднання можливе, але не додасть нової інформації, оскільки Task_3 вже покриває всі унікальні випадки Task_2. Корисно лише для створення збалансованого тренувального набору.

Ваші питання до даних:

6. Чи є в даних колонки які не несуть жодного значення?
Код:
```python
const = t3.columns[t3.nunique() <= 1].tolist()
print(f"Константні колонки: {const}")
```
Результат: ['V3', 'V4', 'V5', 'V14'].
Інтепретація: Ці колонки мають однакове значення для всіх рядів і не допомагають у класифікації.

7. Наскільки дані у Task_2 та Task_3 є розрідженими?
Код:
```python
sparsity = (t3 == 0).mean().mean()
print(f"Розрідженість: {sparsity:.2%}")
```
Результат: 43.15%.
Інтепретація: Майже половина значень — це нулі, що вказує на специфічну природу ознак (події, що виникають рідко).

8. Яка реальна складність даних (аналіз PCA)?
Код:
```python
from sklearn.decomposition import PCA
pca = PCA().fit(t3.drop(columns=['Vclass', 'V3', 'V4', 'V5', 'V14']))
print(f"К-сть компонент для 95% дисперсії: {(pca.explained_variance_ratio_.cumsum() < 0.95).sum() + 1}")
```
Результат: 4 компоненти.
Інтепретація: Незважаючи на 21 ознаку, 95% інформації зосереджено лише в 4-х вимірах.

9. Яка ознака має найсильніший зв'язок з цільовим класом?
Код:
```python
print(t3.corr()['Vclass'].abs().sort_values(ascending=False).index[1])
```
Результат: V6.
Інтепретація: Ознака V6 має найбільший лінійний вплив на результат класифікації.

10. Чи є в даних ознаки, що дублюють одна одну (мультиколінеарність)?
Код:
```python
corr = t3.drop(columns=['V3', 'V4', 'V5', 'V14']).corr().abs()
print(f"Пари з кореляцією > 0.95: {(corr > 0.95).sum().sum() - 17}")
```
Результат: Знайдено 6 таких взаємозв'язків.
Інтепретація: В даних присутня висока мультиколінеарність (наприклад, V11 та V13), що вказує на надмірність деяких параметрів.
